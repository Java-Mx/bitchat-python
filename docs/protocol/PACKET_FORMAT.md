# Packet Format

> **UNVERIFIED**: The exact packet layout, header format, field sizes, byte ordering, payload structure, and maximum packet sizes must be reverse-engineered and documented from the Rust reference implementation.

## To Be Determined
- Header structure and size
- Endianness (Byte ordering)
- Payload structure
- Maximum Transmission Unit (MTU) constraints and maximum payload sizes

*Note: Do NOT invent any packet layouts here. This document will be updated once verified against the reference.*

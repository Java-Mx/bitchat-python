# macOS Platform Support

> **UNTESTED**

## BLE Considerations
- Requires macOS 10.15+
- Uses CoreBluetooth framework via the `bleak` library.
- May require specific privacy permissions (Bluetooth access) in System Settings.
